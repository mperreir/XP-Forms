{
  "name": "Ptrans",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd serveur && npm start",
    "prod": "npm run build && npm start",
    "install-all": "npm install && cd client && npm install && cd ../serveur && npm install"
  }
}
