{
  "name": "Ptrans",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^7.6.0",
    "file-saver": "^2.0.5",
    "jszip": "^3.10.1"
  },
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd serveur && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install && cd ../serveur && npm install"
  }
}
